spring.application.name=Sunnybank

# Database configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST}:5432/${DB_SUNNYBANK}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Exception configuration data server
#server.error.include-stacktrace=never

# Security JWT secret key
sunny.security.oauth0.jwt.secret-key=${SUNNY_SECRET_KEY:PVpKFUZPBgifLi2QuCBbHUhsTlObPb9c3aJxq5g0UYS74fbot5}